export default async function EditTemplatePage() {
  return (
    <main className="flex flex-1 items-center justify-center p-8">
      <p className="text-foreground-muted">
        Form edit template — menyusul di fase berikutnya.
      </p>
    </main>
  );
}