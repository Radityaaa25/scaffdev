export default function LoginPage() {
  return (
    <main className="flex flex-1 items-center justify-center p-8">
      <p className="text-foreground-muted">
        Login admin — menyusul di fase berikutnya (Supabase Auth).
      </p>
    </main>
  );
}