{
  "name": "@scaff/database",
  "version": "0.1.0",
  "private": true,
  "description": "Shared schema Supabase untuk Scaff — metadata template, integrasi, dan admin_users.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@scaff/config": "workspace:*",
    "typescript": "^5.9.3"
  },
  "files": [
    "schema.sql",
    "src"
  ]
}